# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_synth.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKSYNTH_INC)
PKG_LIBS = -L$(WKSYNTH_LIB) -l:wickra_synth.dll
